/*
import {Pipe, PipeTransform} from "@angular/core";
@Pipe({name: 'absenceType'})
export class AbsenceTypePipe implements PipeTransform {
  transform(absenceType: string, ...args: any[]): string {
    let type;
    switch (absenceType) {
      case 'ILLNESS':
        type = 'מחלה';
        break;
      case 'HOLIDAY':
        type= 'חג';
        break;
      case 'VACATION':
        type = 'חופשה';
        break;
      case 'ARMY':
        type = 'מלוים';
        break;
      default:
        type = '';
    }
    return type;
  }
}
*/
